import { DataDictFormatePipe } from './dataDictFormate.pipe';

describe('DataDictFormatePipe', () => {
  it('create an instance', () => {
    const pipe = new DataDictFormatePipe();
    expect(pipe).toBeTruthy();
  });
});
